"use client";
import { useState } from "react";
import { saveActiveChargerId, saveChargers } from "../lib/appStorage";
export function SetupModal({ onSaved }: { onSaved: () => void }) { const [id,setId]=useState(""); const [name,setName]=useState("My Charger"); const submit=()=>{ const clean=id.trim(); if(!/^[a-z0-9_-]{3,32}$/i.test(clean)) return; saveChargers([{id:clean,name:name.trim()||clean}]); saveActiveChargerId(clean); onSaved(); }; return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/70 p-5"><form onSubmit={e=>{e.preventDefault();submit()}} className="w-full max-w-md rounded-3xl border border-white/10 bg-[#10171b] p-7 text-white"><div className="text-xs tracking-[.2em] text-cyan-300">WELCOME TO EVERGY</div><h1 className="mt-3 text-2xl font-semibold">Connect your charger</h1><p className="mt-2 text-sm text-zinc-400">Enter the model or device ID from the original app.</p><input autoFocus value={id} onChange={e=>setId(e.target.value)} placeholder="b8351" className="mt-6 w-full rounded-xl border border-white/10 bg-white/5 px-4 py-3 outline-none focus:border-cyan-400"/><input value={name} onChange={e=>setName(e.target.value)} placeholder="Name" className="mt-3 w-full rounded-xl border border-white/10 bg-white/5 px-4 py-3 outline-none focus:border-cyan-400"/><button className="mt-5 w-full rounded-xl bg-cyan-400 px-4 py-3 font-semibold text-black">Connect</button></form></div> }
